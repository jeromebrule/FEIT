language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-feit' ]; then cd .. && eval "mv $currentfolder generator-feit" && cd generator-feit; fi

